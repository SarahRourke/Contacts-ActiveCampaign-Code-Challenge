import React from 'react';
import '../App.css';

const Deals = (props) => {
    console.log(props)

    
    return (
        <>{props}</>
        // <>
        // {props.map(deal => (
        //     deal.value
        // ))}        </>
    )
}

export default Deals;